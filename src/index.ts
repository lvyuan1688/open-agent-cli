// open-agent-cli — public entry point.
// Re-exports the provider abstraction and MCP client so downstream users
// can import from a single path.

export * from "./provider.js";
export * from "./mcp.js";
